#Tuples
# type of data structure a
# a container to store different values similar to a list
# a structure store multiple pieces of information
# cannot be changed or modified once it is created
# indexed starting at zero
# to store data that never changes (coordinates)


coordinates = (4, 5)  # create a tuple
coordinates2 = [(4, 5), (6, 7), (80, 34)]  # tuples in a list

#coordinates[1] = 10  # cannot do - error out

print(coordinates[0])
print(coordinates2[0])
print(coordinates2[1])
print(coordinates2[2])